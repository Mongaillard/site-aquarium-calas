{
  "name": "@sdv/cli",
  "version": "0.0.1",
  "private": true,
  "description": "Ligne de commande de la simulation de vie",
  "type": "module",
  "bin": {
    "sim": "./dist/main.js"
  },
  "scripts": {
    "build": "tsc -b",
    "typecheck": "tsc -b",
    "sim": "tsc -b && node dist/main.js"
  },
  "dependencies": {
    "@sdv/core": "workspace:*"
  }
}
